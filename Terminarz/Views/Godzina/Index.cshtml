@model IEnumerable<Terminarz.Models.Godzina>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DzienTygodnia.Dzien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GodzinaWizyty)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DzienTygodnia.Dzien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GodzinaWizyty)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GodzinaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GodzinaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GodzinaId })
        </td>
    </tr>
}

</table>
